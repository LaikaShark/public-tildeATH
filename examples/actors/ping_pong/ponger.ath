// Print pong and return the ball for each receive, until the inbox closes.
BIFURCATE ARGS[BALLS, ACKS];
RECV FROM BALLS AS M;
~ATH(M)
{
    PRINT pong;
    SEND M TO ACKS;
    RECV FROM BALLS AS M;
}
THIS.DIE();

// A "tick" lives 0.001-0.01 seconds. The ~ATH loop spin-waits until the
// runtime observes the deadline has passed (typically a few ms).

IMPORT TICK T;
~ATH(T)
{
    // tight spin
}
PRINT tick is dead;
THIS.DIE();

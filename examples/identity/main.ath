// Tests that a function returning its ARGS works, and that the caller
// can decompose the returned object back into its halves.

IMPORTF "id.ath" AS ID;
INPUT LINE;
ID LINE[HEAD, TAIL];
// HEAD is the first-char atom of `line`; TAIL is the rest of the string.
PRINT $TAIL;
PRINT done;
THIS.DIE();

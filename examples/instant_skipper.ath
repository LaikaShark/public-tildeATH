// the "instant" library entry has lifetime [0, 0] — born dead — so the
// ~ATH body never runs. demonstrates that import metadata is now
// semantically meaningful when it matches a library concept.

IMPORT INSTANT V;
~ATH(V)
{
    PRINT never reached;
}
PRINT fell through;
THIS.DIE();

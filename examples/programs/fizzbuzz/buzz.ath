// Print "buzz", then if the counter ARGS encodes n > 1, recurse into FIZZBUZZ with n-1.
PRINT buzz;
BIFURCATE ARGS[LEFT, REST];
~ATH(LEFT)
{
    FIZZBUZZ REST[X, Y];
    BIFURCATE NULL[JUNK, LEFT];
}
THIS.DIE();

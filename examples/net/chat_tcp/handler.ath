// Read client name, then relay tagged lines into the transcript until hangup.
BIFURCATE ARGS[CONN, LOG];
RECV FROM CONN AS NAME;
RECV FROM CONN AS LINE;
~ATH(CONN)
{
    TEXT "[" NAME "] " LINE AS MSG;
    SEND MSG TO LOG;
    RECV FROM CONN AS LINE;
}
THIS.DIE();

// Accept one connection, echo each line back until the client hangs up.
LISTEN "unix:/tmp/ath_echo.sock" AS SERVER;
ACCEPT FROM SERVER AS CONN;
~ATH(CONN)
{
    RECV FROM CONN AS LINE;
    SEND LINE TO CONN;
}
CLOSE SERVER;
THIS.DIE();

// Uppercase each line and send it back until the client hangs up.
IMPORTF <upper> AS UPPER;
LISTEN "unix:/tmp/ath_upper.sock" AS SERVER;
ACCEPT FROM SERVER AS CONN;
~ATH(CONN)
{
    RECV FROM CONN AS LINE;
    UPPER [LINE, NULL]CAPS;
    SEND CAPS TO CONN;
}
CLOSE SERVER;
THIS.DIE();
